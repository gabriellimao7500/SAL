{
  "name": "sal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix Frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix Frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix Frontend",
    "install:all": "npm install && npm install --save-dev vite --prefix Frontend"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "body-parser": "^1.20.2",
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0",
    "nodemailer": "^6.9.14",
    "nodemon": "^3.1.4",
    "sweetalert2": "^11.15.10",
    "swiper": "^11.1.9"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}